fun main() {
    var input = readLine()!!.toString()
    var quackArray = Array<Array<String>>(input.length){ arrayOf()}
    var i =0
    while(i < input.length) {
        if(input[i] == 'q') {

        }
    }

}